export enum cognitiveSkills  {
    "none" = 1,
}

export enum conduct {
    "buena" = 1,
    "regular" = 2,
    "mala" = 3,
}

export enum socialBasic {
    "none" = 1,
}

export enum socialAdvanced {
    "none" = 1,
}

export enum socialFeelings {
    "none" = 1,
}

export enum socialAssaulta {
    "none" = 1,
}

export enum socialStress {
    "none" = 1,
}

export enum socialPlanning {
    "none" = 1,
}

export enum alternativeSkills {
    "none" = 1,
}